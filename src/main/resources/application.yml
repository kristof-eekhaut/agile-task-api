
spring:
  datasource:
    url: jdbc:mysql://192.168.99.101:3307/agile_tasks
    username: root
    password: root
    platform: mysql
    initialize: false

  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect

  jackson:
    deserialization:
      fail-on-unknown-properties: true


---

spring:
  profiles: container
  datasource:
    url: jdbc:mysql://${DATABASE_HOST}:${DATABASE_PORT}/${DATABASE_NAME}
    username: ${DATABASE_USER}
    password: ${DATABASE_PASSWORD}
    initialize: true
